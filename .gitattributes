# 统一换行符：仓库与工作区一律 LF，保证 Windows / macOS / Linux 检出后逐字节一致。
# （PowerShell 可直接执行 LF 换行的 .ps1，本项目 build_exe.ps1 已实测通过）
* text=auto eol=lf

# 二进制文件不做行尾转换
*.ico   binary
*.exe   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.webp  binary
*.epub  binary
